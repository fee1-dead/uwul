fn a(a: i32) -> i32 {
    {
        while true {
            hello;
        } * b;
        let b;
        let c = "e";
        let a = b;
        a = if -{c} { 1 } else { 2 };
        println("lol");
    }
    println(a);
}